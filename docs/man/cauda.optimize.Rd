% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauda-docs.R
\name{cauda.optimize}
\alias{cauda.optimize}
\title{Recommend optimal settings for controllable input variables}
\usage{
cauda.optimize(
  df,
  target,
  controls,
  maximize = FALSE,
  n_grid = 10,
  actionable = NULL
)
}
\arguments{
\item{df}{A cleaned numeric data frame (output of \code{cauda.prep()}).}

\item{target}{Character. The outcome variable to optimize.}

\item{controls}{Character vector. Variables to search over.}

\item{maximize}{Logical. If TRUE, maximize the target. If FALSE, minimize. Default is FALSE.}

\item{n_grid}{Integer. Number of grid points per variable. Default is 10.}

\item{actionable}{Character vector. Subset of controls that are truly actionable. Default is NULL.}
}
\value{
Invisibly returns a data frame with the optimal settings and predicted outcome.
}
\description{
Fits a random forest model and performs a grid search over controllable
input variables to find the combination that minimizes or maximizes the target.
}
\examples{
df_ready <- cauda.prep(mtcars, verbose = FALSE)
cauda.optimize(df_ready, target = "mpg", controls = c("hp", "wt"),
               actionable = c("hp"), maximize = TRUE)

}
