% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauda-docs.R
\name{cauda.independence}
\alias{cauda.independence}
\title{Conditional independence tests between variable pairs}
\usage{
cauda.independence(df, target = NULL, threshold = 0.05, verbose = TRUE)
}
\arguments{
\item{df}{A cleaned numeric data frame (output of \code{cauda.prep()}).}

\item{target}{Character. If provided, only tests pairs involving this variable. Default is NULL.}

\item{threshold}{Numeric. P-value threshold for flagging significant dependencies. Default is 0.05.}

\item{verbose}{Logical. Print full results table. Default is TRUE.}
}
\value{
Invisibly returns a data frame with columns Var1, Var2, p_value, and Result.
}
\description{
Tests whether pairs of variables are conditionally independent given all
other variables. A low p-value indicates genuine dependence even after
controlling for other variables.
}
\examples{
df_ready <- cauda.prep(mtcars, verbose = FALSE)
cauda.independence(df_ready, target = "mpg")

}
