% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauda-docs.R
\name{cauda.analyze}
\alias{cauda.analyze}
\title{Run the full cauda analysis pipeline in one command}
\usage{
cauda.analyze(df, highlight = NULL, verbose = TRUE)
}
\arguments{
\item{df}{A raw data frame.}

\item{highlight}{Character. Column name to highlight across all outputs. Default is NULL.}

\item{verbose}{Logical. Print summaries throughout. Default is TRUE.}
}
\value{
Invisibly returns a list with data, dag, cor_mat, and pcor_mat.
}
\description{
Convenience wrapper that runs the complete analysis pipeline:
missing value summary, data prep, causal DAG, correlation heatmap and network,
and partial correlation network.
}
\examples{
results <- cauda.analyze(mtcars, highlight = "mpg")

}
