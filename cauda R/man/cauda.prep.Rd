% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauda-docs.R
\name{cauda.prep}
\alias{cauda.prep}
\title{Recode and clean a data frame in one step}
\usage{
cauda.prep(
  df,
  max_levels = 10,
  nzv_thresh = 0.01,
  cor_thresh = 0.95,
  na_action = "omit",
  verbose = TRUE
)
}
\arguments{
\item{df}{A raw data frame.}

\item{max_levels}{Integer. Passed to \code{cauda.recode()}. Default is 10.}

\item{nzv_thresh}{Numeric. Passed to \code{cauda.clean()}. Default is 0.01.}

\item{cor_thresh}{Numeric. Passed to \code{cauda.clean()}. Default is 0.95.}

\item{na_action}{Character. Passed to \code{cauda.clean()}. Default is "omit".}

\item{verbose}{Logical. Print progress. Default is TRUE.}
}
\value{
A cleaned, fully numeric data frame ready for analysis.
}
\description{
Convenience wrapper that runs \code{cauda.recode()} followed by \code{cauda.clean()}.
This is the recommended first step before any analysis.
}
\examples{
df_ready <- cauda.prep(mtcars, verbose = FALSE)

}
