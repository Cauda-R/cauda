% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauda-docs.R
\name{cauda.corr}
\alias{cauda.corr}
\title{Correlation heatmap and spring network (side by side)}
\usage{
cauda.corr(df, threshold = 0.15, highlight = NULL, verbose = TRUE)
}
\arguments{
\item{df}{A cleaned numeric data frame (output of \code{cauda.prep()}).}

\item{threshold}{Numeric. Minimum absolute correlation to show in the network. Default is 0.15.}

\item{highlight}{Character. Column name to highlight in red. Default is NULL.}

\item{verbose}{Logical. Print top correlations to console. Default is TRUE.}
}
\value{
Invisibly returns the correlation matrix.
}
\description{
Produces two correlation visualizations side by side: a corrplot heatmap
and a qgraph spring-layout correlation network.
}
\examples{
df_ready <- cauda.prep(mtcars, verbose = FALSE)
cauda.corr(df_ready, highlight = "mpg", verbose = FALSE)

}
